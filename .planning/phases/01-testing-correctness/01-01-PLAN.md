---
phase: 01-testing-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DiffView.ts
  - e2e/test/specs/external-change.e2e.ts
autonomous: true
requirements: [TEST-01, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07]

must_haves:
  truths:
    - "Accept-all writes the NEW (external) content to the vault file, not the old content"
    - "Reject-all restores the ORIGINAL content to the vault file"
    - "External file change automatically opens the diff view tab"
    - "Expanding a folded unchanged region expands in both editors simultaneously"
    - "Multiple sequential external edits accumulate into one diff that accepts correctly"
    - "The DiffView.ts side-swap fix is committed with tests proving correctness"
  artifacts:
    - path: "e2e/test/specs/external-change.e2e.ts"
      provides: "Strengthened e2e tests with content verification assertions"
      contains: "readFileFromVault"
    - path: "src/DiffView.ts"
      provides: "Committed side-swap fix (A=new, B=old)"
      contains: "newContent"
  key_links:
    - from: "e2e/test/specs/external-change.e2e.ts"
      to: "e2e/test/pageobjects/ObsidianApp.ts"
      via: "Page object methods for vault interaction"
      pattern: "ObsidianApp\\.(readFileFromVault|clickAccept|modifyFileExternally)"
---

<objective>
Commit the DiffView.ts side-swap fix and strengthen all existing e2e tests with content verification assertions.

Purpose: The existing test suite has structural assertions (e.g., "section exists") but lacks content verification (e.g., "accepted file contains NEW content"). This plan strengthens those tests so they prove functionality is correct, then commits the side-swap fix with tests as evidence.

Output: Committed side-swap fix in DiffView.ts, strengthened tests for accept-all, reject-all, external detection, fold sync, and accumulated diffs.
</objective>

<execution_context>
@/Users/ryanbrown/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanbrown/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-testing-correctness/01-CONTEXT.md
@.planning/phases/01-testing-correctness/01-RESEARCH.md

@e2e/test/specs/external-change.e2e.ts
@e2e/test/pageobjects/ObsidianApp.ts
@src/DiffView.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the DiffView.ts side-swap fix</name>
  <files>src/DiffView.ts</files>
  <action>
  The uncommitted changes in src/DiffView.ts swap old/new content sides in MergeView and rename the revert tooltip. This is a 3-line fix:
  - Side A (editable, left) gets `newContent` (was `oldContent`)
  - Side B (read-only, right) gets `oldContent` (was `newContent`)
  - Tooltip changes from "Apply this change" to "Revert this change"

  Review the existing `git diff src/DiffView.ts` to confirm these are the only changes. Stage and commit with message: `fix: swap MergeView sides so accept applies new content`.

  This MUST be committed before strengthening tests so that TEST-01 (accept applies new content) will pass.
  </action>
  <verify>
    <automated>cd /Users/ryanbrown/code/obsidian-diff-viewer && git log --oneline -1 | grep -q "swap MergeView" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>DiffView.ts side-swap fix committed. git log shows the commit. No other uncommitted changes in src/DiffView.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen existing e2e tests with content verification</name>
  <files>e2e/test/specs/external-change.e2e.ts</files>
  <action>
  Modify the existing test file to strengthen assertions. Do NOT delete or rename existing tests -- only add/strengthen assertions within them. For each requirement:

  **TEST-01 (accept-all applies new content):** Find the existing accept test (test #3, likely "should apply changes when Accept is clicked" or similar). After `clickAccept(0)` and a pause, add: read file from vault via `ObsidianApp.readFileFromVault(filePath)` and assert it equals the NEW/external content string, not the original. This proves accept writes new content.

  **TEST-07 (reject-all restores original):** Find the existing reject test (test #4, likely "should revert to old content when Reject is clicked" or similar). After the reject action and a pause, add: read file from vault and assert it equals the ORIGINAL content string (before external modification). This proves reject restores old content.

  **TEST-03 (external change opens diff):** Review existing test #1 ("should open diff tab when a file is modified externally"). It likely already asserts diff tab opens. Verify it checks that the diff section exists with at least one section. If assertions are adequate, leave it alone. If it only checks tab existence without section content, add a section count assertion.

  **TEST-04 (fold sync across editors):** Find existing test #6 about fold/expand. Strengthen by scoping fold widget selectors to each editor separately:
  - Before expand: count `.cm-merge-a .diff-view-fold-widget:not(.diff-view-fold-expanded)` and `.cm-merge-b .diff-view-fold-widget:not(.diff-view-fold-expanded)` -- assert they are equal.
  - After clicking a fold widget: assert BOTH editors have one fewer collapsed fold widget.
  - If `.cm-merge-a` / `.cm-merge-b` selectors don't match, inspect the DOM structure via `browser.execute` and adjust. CM6 MergeView places these classes on the editor root elements.

  **TEST-05 (accumulated diffs resolve):** Find existing test #7 about sequential/accumulated edits. After the existing assertions, add: call `clickAccept(0)`, pause, then `readFileFromVault` and assert the file content equals the LATEST external edit content. This proves accumulated diffs accept correctly.

  **Timing:** Use `browser.pause(2000)` after accept/reject actions before reading vault files. Use `ObsidianApp.waitForDiffTab()` after external modifications.

  **Test data:** Use clearly distinct strings for original vs external content so assertions are unambiguous. For TEST-04 fold tests, ensure content has enough unchanged lines (20+) to trigger fold regions.
  </action>
  <verify>
    <automated>cd /Users/ryanbrown/code/obsidian-diff-viewer/e2e && npx wdio run ./wdio.conf.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All existing tests pass with strengthened assertions. Tests verify: (1) accept writes new content to vault, (2) reject restores original content, (3) external change opens diff, (4) fold widgets sync across both editors, (5) accumulated diffs accept to latest content. No test regressions.</done>
</task>

</tasks>

<verification>
Run the full e2e test suite: `cd e2e && npx wdio run ./wdio.conf.ts`
All tests must pass. The suite should have the same number of tests as before (no removed tests), with strengthened assertions.
</verification>

<success_criteria>
1. DiffView.ts side-swap fix is committed (TEST-06)
2. Accept test verifies vault file contains NEW content after accept (TEST-01)
3. Reject test verifies vault file contains ORIGINAL content after reject (TEST-07)
4. External change test confirms diff tab opens (TEST-03)
5. Fold test verifies both editors sync fold state (TEST-04)
6. Accumulated diffs test verifies accept writes latest content (TEST-05)
7. Full e2e suite passes green
</success_criteria>

<output>
After completion, create `.planning/phases/01-testing-correctness/01-01-SUMMARY.md`
</output>
