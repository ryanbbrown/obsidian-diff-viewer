---
phase: 01-testing-correctness
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - e2e/test/specs/external-change.e2e.ts
autonomous: false
requirements: [TEST-02]

must_haves:
  truths:
    - "User can click a revert button on an individual diff chunk to undo just that change"
    - "After partial revert, accept-all writes the mixed content (some chunks reverted, some kept) to the vault file"
  artifacts:
    - path: "e2e/test/specs/external-change.e2e.ts"
      provides: "New per-chunk revert test"
      contains: "diff-view-revert-btn"
  key_links:
    - from: "e2e/test/specs/external-change.e2e.ts"
      to: "src/DiffView.ts"
      via: "Revert button DOM interaction triggers MergeView chunk revert"
      pattern: "revert-btn.*click"
---

<objective>
Add a new e2e test for per-chunk accept/reject via CM6 MergeView revert controls, then manually verify all plugin functionality.

Purpose: TEST-02 is the only requirement needing a brand-new test (not strengthening an existing one). Per-chunk revert is critical functionality -- users need to selectively undo individual changes. This plan also includes the manual verification gate required before Phase 2.

Output: New per-chunk revert test passing, manual confirmation of all plugin functionality.
</objective>

<execution_context>
@/Users/ryanbrown/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanbrown/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-testing-correctness/01-CONTEXT.md
@.planning/phases/01-testing-correctness/01-RESEARCH.md
@.planning/phases/01-testing-correctness/01-01-SUMMARY.md

@e2e/test/specs/external-change.e2e.ts
@e2e/test/pageobjects/ObsidianApp.ts
@src/DiffView.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-chunk revert e2e test</name>
  <files>e2e/test/specs/external-change.e2e.ts</files>
  <action>
  Add a new test for per-chunk accept/reject via CM6 revert controls. Place it alongside the existing tests in the same describe block.

  **Test setup:** Create a note with 8+ lines of distinct content (e.g., "alpha", "beta", "gamma", "delta", "epsilon", "zeta", "eta", "theta"). Modify externally to change line 1 and line 8 (two separate diff chunks with unchanged lines between them). Wait for diff tab.

  **Revert interaction:** Find revert buttons via `$$(".diff-view-revert-btn")`. Assert at least 2 exist (one per chunk). Scroll the first button into view and click it. Pause 500ms. This reverts chunk 1 (line 1 goes back to original "alpha").

  **Verify mixed content:** Click accept-all via `ObsidianApp.clickAccept(0)`. Pause 2000ms. Read vault file. Assert content equals original lines BUT with line 8 changed to "THETA" (chunk 2 kept, chunk 1 reverted).

  **If revert button click doesn't work** (CM6 widget event swallowing): Fall back to `browser.execute()` to dispatch a click event directly on the button element, or use the MergeView API `acceptChunk` if accessible. See RESEARCH.md Pitfall 1 and Open Question 1 for details.

  **If `.diff-view-revert-btn` selector doesn't match:** Inspect the DOM via `browser.execute(() => document.querySelector('.cm-merge-revert')?.innerHTML)` to find the correct selector structure. CM6 does NOT use shadow DOM. See RESEARCH.md Open Question 2.

  **Test name:** Use a descriptive name like "should allow per-chunk revert via revert controls" so it can be targeted with `--mochaOpts.grep "per-chunk revert"`.
  </action>
  <verify>
    <automated>cd /Users/ryanbrown/code/obsidian-diff-viewer/e2e && npx wdio run ./wdio.conf.ts --mochaOpts.grep "per-chunk revert" 2>&1 | tail -20</automated>
  </verify>
  <done>New per-chunk revert test passes. Clicking a revert button on one chunk reverts only that chunk. Accept-all after partial revert writes mixed content (reverted + kept changes) to vault file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification gate for Phase 1</name>
  <files>N/A</files>
  <action>
  Deploy plugin to vault and manually verify all functionality:

  1. Run full test suite: `cd e2e && npx wdio run ./wdio.conf.ts` -- all tests should pass
  2. Build and deploy: `npm run build`, copy main.js/manifest.json/styles.css to ~/vault/.obsidian/plugins/obsidian-diff-viewer/, restart Obsidian
  3. Manual walkthrough:
     a. Edit a note externally -- diff view should open automatically
     b. Click "Accept All" -- file should contain NEW external content
     c. Repeat, click "Reject All" -- file should contain ORIGINAL content
     d. Multi-change diff -- verify revert buttons, click one, accept -- verify mixed result
     e. Diff with enough unchanged lines for folding -- click fold widget -- both sides expand in sync
     f. Two sequential external edits -- verify accumulation, accept works
  4. If issues found, describe them so code and tests can be fixed
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User confirms all plugin functionality works correctly in real Obsidian usage. Phase 1 verification gate passed.</done>
</task>

</tasks>

<verification>
Run the full e2e test suite: `cd e2e && npx wdio run ./wdio.conf.ts`
All tests must pass including the new per-chunk revert test.
Manual verification confirms real-world behavior matches test expectations.
</verification>

<success_criteria>
1. Per-chunk revert test passes (TEST-02)
2. Full e2e suite is green (all tests pass)
3. Manual verification confirms: accept writes new content, reject restores old, per-chunk revert works, fold syncs, accumulated diffs resolve
4. User approves at checkpoint before Phase 2 proceeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-testing-correctness/01-02-SUMMARY.md`
</output>
